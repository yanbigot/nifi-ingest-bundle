package com.socgen.nifi.processors.training

/**
 * Should be able to either output a result
 *  - with inline endpoints values
 *  - build a functionally congruent object
 */
class Training {
  /**
   * Bean with nested children
   */
}
